//
//  IfSwitchStatements.h
//  121 Review
//
//  Created by Kyle Parker on 10/7/23.
//

#ifndef IfSwitchStatements_h
#define IfSwitchStatements_h

#include <stdio.h>

inline int ifStatements(int input) {
	/// Write an if statements (with needed else/elseif clasuse(s)) to
	/// return 1 if the number (input) is less than or equal to 10, 2 if num ≤ 20,
	/// 3 if num ≤ 30 and so on until 10 if num ≤ 100. If it does not fit into
	/// any of the cases listed, then it needs to return that number back.
	
	return (int)NAN;
}

inline char * switchStatements(int input) {
	/// Write a switch statement that will return "one" for 1, "two" for 2,
	/// and so on until 10. Return NULL for any other case. Everything
	/// should be in the switch statements. (no return statements should
	/// run outside of the switch statement). If you want to use flags,
	/// you must use strcpy and malloc, so I reccomend using directly
	/// returning your values for simplicity.
	
	
	return NULL;
}

inline int logicStatements(void) {
	/// Write code that will sum every number from 1 to 100 using a DO WHILE LOOP
	///

	return (int)NAN;
}

#endif /* IfSwitchStatements_h */
